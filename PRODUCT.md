# 한자어 훈음 분해 도구

## 한 줄 요약

한자어를 검색하면 한자 표기와 **글자별 훈음**을 한눈에 보여준다.

## 왜 만드나

네이버·다음 사전은 `심도 → 深度`까지만 보여준다. 深이 무슨 심자이고 度가 무슨 도자인지 알려면 한자사전으로 두 번 더 들어가야 한다. 그 두 번을 없애는 것이 이 도구의 존재 이유다.

## 입력과 출력

```
입력: 심도

출력:
  심도  深度
  深  깊을 심
  度  법도 도
  뜻: 깊은 정도
```

가로로 나란히 배치해서 시선이 좌→우로 한 번만 흐르게 한다.

---

## 데이터 출처

### 1. 단어 → 한자 표기 + 뜻풀이

표준국어대사전 오픈 API

```
https://stdict.korean.go.kr/api/search.do
  ?key={인증키}
  &q={검색어}
  &req_type=json
```

사용할 필드

| 필드               | 내용                       |
| ------------------ | -------------------------- |
| `word`             | 표제어                     |
| `sup_no`           | 어깨번호 (동형이의어 구분) |
| `origin`           | 한자 표기                  |
| `sense.definition` | 뜻풀이                     |
| `pos`              | 품사                       |

- `sense`는 **항상 단일 객체**다. 배열 처리 불필요
- 응답 값에 **앞뒤 공백이 붙어서 온다.** 반드시 trim 처리
- 하루 5만 건 제한

### 2. 한자 → 훈음

한국어문회 급수별 한자 CSV

```
https://raw.githubusercontent.com/rycont/hanja-grade-dataset/main/hanja.csv
```

- 5,978자. 프로젝트에 JSON으로 변환해 포함한다 (런타임 다운로드 금지)
- `meaning` 필드는 `[[['깊을'], ['심']]]` 형태의 문자열. 파싱 필요
- 훈음이 여러 개면 `[[['법도'], ['도']], [['헤아릴'], ['탁']]]`
- 부수(`radical`), 총획수(`total_strokes`), 급수(`level`)도 함께 제공됨

---

## 처리 흐름

각 단계를 **독립된 함수**로 분리한다. 나중에 앞뒤로 기능을 붙이기 위해서다.

```
1. 사전 조회      단어 → [{한자표기, 뜻풀이, 어깨번호}, ...]
2. 정규화         한자 문자열 → NFC 정규화
3. 분해           표기를 한 글자씩 자름
4. 훈음 매칭      각 글자 + 한글 독음 → 훈음 선택
5. 렌더링         화면에 표시
```

---

## 이미 확인된 함정

### ① 호환 한자 (필수 처리)

CSV의 일부 한자가 유니코드 호환 영역 코드로 저장되어 있다. 눈으로는 같지만 코드값이 달라 조회에 실패한다.

```
樂  U+F95C (CSV)  ≠  樂  U+6A02 (일반 입력)
```

해당 글자 9자: `車 串 金 樂 率 不 泌 塞` 등. 자주 쓰이는 글자가 많다.

**대응:** CSV를 JSON으로 변환할 때, 그리고 API 응답의 `origin`을 받을 때 모두 NFC 정규화를 통과시킨다.

```js
str.normalize("NFC");
```

### ② 두음법칙

단어 첫 글자는 사전 독음과 실제 독음이 다를 수 있다.

```
女子 → "여자"인데 女의 독음은 "녀"
理由 → "이유"인데 理의 독음은 "리"
```

**대응:** 첫 글자 매칭 시 ㄹ→ㄴ/ㅇ, ㄴ→ㅇ 변환표를 함께 대조한다.

### ③ 속음 (처리 불필요)

`十月 → 시월`, `六月 → 유월`, `許諾 → 허락` 등은 매칭에 실패한다.

**대응 불필요.** 이 글자들은 모두 훈음이 하나뿐이라 선택의 여지가 없고, 화면에는 정확한 값이 나온다. 매칭 실패 플래그만 무시하면 된다.

### ④ 다음자는 24자뿐

훈음이 2개 이상인 글자는 전체의 4.4%(262자)이며, 그중 실사용에서 자주 만나는 것은 아래 24자다.

```
降 車 見 更 金 度 讀 洞 樂 復 北 殺 狀 說 省 宿 惡 易 切 便 布 暴 行 畫
```

매칭 로직이 중요한 것은 이 글자들뿐이다. 나머지는 매칭 없이 첫 번째 훈음을 써도 정확하다.

### ⑤ 동형이의어 (v1에서는 나열)

한글이 같고 한자가 다른 단어가 여러 개 존재한다.

```
심도 ①  心到   독서 태도
심도 ②  深到   깊은 곳에 닿음
심도 ③  深度   깊은 정도
심도 ④  深悼   마음속 깊이 슬퍼함
```

**v1 대응:** 전부 카드로 나열한다. 어느 것이 흔한지 자동 판별하는 로직은 만들지 않는다. 실사용 데이터가 쌓인 뒤에 판단한다.

### ⑥ 결과 없음은 JSON이 아니라 빈 응답으로 온다 (필수 처리)

검색어가 사전에 없을 때 stdict는 `{"channel": {...}}` 형태의 빈 JSON을 주지 않는다. **HTTP 200 + 본문 길이 0**으로 응답한다.

```
정상 검색   HTTP 200, body: {"channel": {"item": [...]}}
결과 없음   HTTP 200, body: "" (길이 0)
```

**대응:** `res.json()`을 바로 부르면 `SyntaxError: Unexpected end of JSON input`으로 죽는다. `res.text()`로 먼저 받아서 빈 문자열이면 "결과 없음"으로 분기하고, 비어있지 않을 때만 `JSON.parse`한다. 빈 응답을 에러로 오분류하면 진짜 에러(네트워크 실패 등)와 구분이 안 된다.

---


## 기술 구성

- 정적 사이트 (프레임워크는 자유)
- 훈음 데이터는 빌드 시점에 JSON으로 번들링
- API 키는 serverless function으로 감싸 노출 방지
- 단어장 저장은 localStorage
- 서버 DB 없음

### 스크립트 (`scripts/`)

npm script로는 등록돼 있지 않다. 필요할 때 수동으로 `node scripts/파일명`으로 실행한다.

| 파일 | 하는 일 |
|---|---|
| `build-hanja.mjs` | 한국어문회 CSV를 받아 `src/data/hanja.json`으로 변환 (1단계 산출물). 원본 CSV가 바뀌거나 데이터를 다시 뽑아야 할 때만 실행 |
| `test-match.mjs` | PRODUCT.md "검증 케이스" 표의 14개 케이스를 `hanja.ts` 매칭 로직과 별도로 재현해 회귀 테스트 |

---

## 검증 케이스

로직 완성 후 아래가 전부 통과해야 한다. (실제 데이터로 검증 완료된 목록)

| 입력     | 기대 결과                                         | 검증 포인트                 |
| -------- | ------------------------------------------------- | --------------------------- |
| 심도     | 深 깊을 심 / 度 법도 도                           | 기본                        |
| 촌탁     | 忖 헤아릴 촌 / 度 헤아릴 탁                       | 度 다음자 구분              |
| 낙원     | 樂 즐길 락 / 園 동산 원                           | 樂 다음자 + 호환한자        |
| 음악     | 音 소리 음 / 樂 노래 악                           | 樂 다른 음                  |
| 요산요수 | 樂 좋아할 요 / 山 메 산 / 樂 좋아할 요 / 水 물 수 | 같은 글자 반복              |
| 여자     | 女 계집 녀 / 子 아들 자                           | 두음법칙                    |
| 이유     | 理 다스릴 리 / 由 말미암을 유                     | 두음법칙                    |
| 노인     | 老 늙을 로 / 人 사람 인                           | 두음법칙                    |
| 확률     | 確 굳을 확 / 率 비율 률                           | 率 다음자 + 호환한자        |
| 인솔     | 引 끌 인 / 率 거느릴 솔                           | 率 다른 음                  |
| 자동차   | 自 스스로 자 / 動 움직일 동 / 車 수레 차          | 3글자 + 車 다음자           |
| 유치원   | 幼 어릴 유 / 稚 어릴 치 / 園 동산 원              | 3글자                       |
| 불가능   | 不 아닐 불 / 可 옳을 가 / 能 능할 능              | 不 호환한자                 |
| 시월     | 十 열 십 / 月 달 월                               | 속음 — 실패해도 결과는 정확 |

---

## 작업 순서

| 단계 | 내용                                   | 산출물           |
| ---- | -------------------------------------- | ---------------- |
| 1    | CSV → JSON 변환 (NFC 정규화 포함)      | `hanja.json`     |
| 2    | 매칭 함수 작성 + 검증 케이스 통과      | 화면 없음        |
| 3    | API 연동 (프록시 포함)                 | 콘솔에 결과 출력 |
| 4    | 최소 화면 (input + div, 스타일 없음)   | 동작하는 페이지  |
| 5    | **3~5일 직접 사용, 안 되는 단어 수집** | 문제 목록        |
| 6    | 디자인 + 발견된 문제 수정              | v1               |

5단계를 건너뛰지 않는다. 상상으로 만든 엣지케이스 목록보다 실사용 3일이 훨씬 정확하다.

---

## AI 코딩 도구에 대한 지시

- 한자 표기는 API 응답의 `origin`에서만 가져온다
- 훈음은 `hanja.json`에서만 조회한다
- 데이터에 없으면 "훈음 정보 없음"으로 표시한다

**단계별로 진행할 것.** 위 작업 순서의 각 단계를 완료하고 검증한 뒤 다음으로 넘어간다. 한 번에 전체를 생성하지 않는다.

---

## 화면 상태별 문구 (검색 결과 화면 제외)

에러 원인은 화면에 노출하지 않는다. 서버 콘솔 로그로만 남기고, 화면엔 아래 문구만 보여준다.

| 상태 | 조건 | 문구 |
|---|---|---|
| 초기 진입 | 아직 검색 안 함 | (문구 없음) |
| 로딩 | 요청 중 | 불러오는 중… |
| 처리 불가 입력 | 숫자·영어·자모·특수문자·문장 등, 한글 단어가 아님 | "한글 단어를 입력해 주세요." |
| 결과 없음 | stdict API가 항목 자체를 못 줌 (오타·신조어·미등재 등, 원인 구분 안 함) | "'{검색어}'에 대한 검색 결과가 없어요." |
| 순우리말 | API는 항목을 줬지만 전부 한자 표기가 없어 필터링됨 | "'{검색어}'는 순우리말이라 한자가 없어요." |
| 오류 | 서버 설정·외부 API 실패 등, 원인 구분 없이 하나로 | "오류가 발생했어요. 잠시 후 다시 시도해 주세요." |

"결과 없음"과 "순우리말"은 API 응답에서 이미 구분되는 신호다 — 전자는 `channel.item` 자체가 없고, 후자는 항목은 있는데 `origin`에 한자가 없다. 지금은 이 구분을 버리고 둘 다 `results: []`로 뭉뚱그리고 있음 — 구분 정보(reason)를 응답에 추가하는 작업 필요.

---

## 개선 백로그

새로 떠오르는 엣지케이스/아이디어는 이 표에 행만 추가한다. 문서가 계속 자랄 걸 감안해 프로즈 대신 표로 유지.

| 항목                        | 내용                                                                  | 상태     |
| --------------------------- | --------------------------------------------------------------------- | -------- |
| 결과없음 사유 구분          | API가 not_found/native_korean reason 반환하도록 수정                  | 완료     |
| 에러 메시지 사용자화        | 원인 노출 대신 친화적 문구 + 서버 로그로 이관                         | 완료     |
| 매칭 실패 로깅              | hanja.json에 없는 글자를 서버 콘솔에 자동 기록 (수동 3~5일 관찰 대체) | 예정     |
| 레이스 컨디션 방지          | 연속 검색 시 이전 응답이 최신 결과를 덮어쓰는 문제                    | 완료 (TanStack Query 도입으로 자동 해결) |
| 다음자 하이라이트           | 훈음이 2개 이상인 글자는 "다른 음도 있음"을 화면에 표시               | 아이디어 |
| 동형이의어 정렬 힌트        | 자동판별 로직 없이 정렬 우선순위만 부여하는 방식 검토                 | 아이디어 |
| 최근 검색어 (localStorage)  | 재방문 유도, 서버 불필요                                              | 아이디어 |
| 이름 풀이 특화 진입점       | 기존 기능 재프레이밍으로 감정적 훅 + 공유 유발                        | 검토중   |
| 헷갈리는 한자어 비교 콘텐츠 | 결재/결제 등 정적 페이지, SEO 유입 목적                               | 검토중   |
| 우리말샘 API 도입 검토      | stdict(표준국어대사전)엔 없는 신조어·합성어(예: 두발자유화)가 우리말샘엔 등재돼 있음. opendict.korean.go.kr 별도 키·스키마 필요, 참여형이라 데이터 품질 검증 필요 | 아이디어 |
